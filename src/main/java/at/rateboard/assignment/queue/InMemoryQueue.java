package at.rateboard.assignment.queue;

/**
 * A Queue Implementation that does persist the data only in memory
 * Since this is identical to the default queue from the standard lib this class does not override anything
 */
public class InMemoryQueue extends PersistedQueue {
}
